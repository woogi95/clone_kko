
const searchbt (x)
const searchBt (ok)
const SearchBt (ok) 정해진 용도
const SEARCH_BT 단순 문자령, 숫자, 정보
const search_bt (o)
const s-e-a-r-ch (x)
const _search (x)

window.addEventListener("DOMContentLoaded")

// DOMContentLoaded > 이미지 파일을 제외한 html 태그를 먼저 읽어준다
  console.log("load 완료");
  //보관함을 만들고
  // 라벨을 붙여주겠다.
  // 그리고 라벨이 붙은 보관함에 재료를 담겠다.
  // 그리고, 제어
  const searchBt = document.querySelector("#search");
  console.log(searchBt);